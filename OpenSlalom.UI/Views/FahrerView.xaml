<UserControl x:Class="OpenSlalom.UI.Views.FahrerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:Background="White">
    <Border Background="Transparent">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock FontWeight="Bold" FontSize="20" Text="Fahrer" VerticalAlignment="Center" />
                <Button Grid.Column="1" Style="{StaticResource RoundedButton}" Background="#1F84DE" Content="Fahrer anlegen" Width="120" Height="30" Click="OpenCreateFahrerPage_OnClick" />
            </Grid>

            <Grid Grid.Row="2">
                <DataGrid x:Name="FahrerDataGrid"
                          ItemsSource="{Binding FahrerItems}"
                          Background="Transparent"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          BorderThickness="0"
                          RowHeaderWidth="0"
                          MinRowHeight="36"
                          VerticalContentAlignment="Center"
                          SelectionMode="Single"
                          AlternationCount="2"
                          ColumnHeaderStyle="{StaticResource StandardDataGridHeaderStyle}"
                          CellStyle="{StaticResource StandardDataGridCellStyle}"
                          RowStyle="{StaticResource StandardDataGridRowStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}" Width="19*" ElementStyle="{StaticResource StandardDataGridTextElementStyle}" />
                        <DataGridTextColumn Header="Nachname" Binding="{Binding Nachname}" Width="19*" ElementStyle="{StaticResource StandardDataGridTextElementStyle}" />
                        <DataGridTextColumn Header="Mitgliedsnummer" Binding="{Binding MitgliedsNummer}" Width="15*" ElementStyle="{StaticResource StandardDataGridTextElementStyle}" />
                        <DataGridTextColumn Header="Geburtsdatum" Binding="{Binding GeburtsdatumText}" Width="14*" ElementStyle="{StaticResource StandardDataGridTextElementStyle}" />
                        <DataGridTemplateColumn Header="Geschlecht" Width="10*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Width="20"
                                           Height="20"
                                           Stretch="Uniform"
                                           Source="{Binding GeschlechtIconPath, Converter={StaticResource SvgResourceToImageSourceConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
						<DataGridTextColumn Header="Verein" Binding="{Binding VereinName}" Width="28*">
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="TextBlock"
									   BasedOn="{StaticResource StandardDataGridTextElementStyle}">
									<Setter Property="TextTrimming" Value="CharacterEllipsis"/>
									<Setter Property="TextWrapping" Value="NoWrap"/>
									<Setter Property="ToolTip" Value="{Binding VereinName}"/>
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>
                        <DataGridTemplateColumn Header="Aktionen" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Height="32" VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="10" />
                                        </Grid.ColumnDefinitions>

                                        <Button Grid.Column="1" Height="28" Style="{StaticResource RoundedButtonLeft}" Background="#1F84DE" Tag="{Binding Id}" Click="OpenEditFahrerPage_OnClick">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" />
                                        </Button>
                                        <Button Grid.Column="2" Height="28" Style="{StaticResource RoundedButtonRight}" Background="Red" Tag="{Binding Id}" Click="OpenDeleteFahrerPage_OnClick">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" />
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border x:Name="FahrerCreatePage" x:FieldModifier="internal" Visibility="Collapsed" Background="#F8FAFC" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8">
                    <StackPanel Margin="20">
                        <TextBlock FontWeight="Bold" FontSize="18" Text="Fahrer anlegen" />
                        <TextBlock Margin="0,16,0,4" Text="Vorname" />
                        <TextBox x:Name="CreateFahrerVornameTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Nachname" />
                        <TextBox x:Name="CreateFahrerNachnameTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Mitgliedsnummer" />
                        <TextBox x:Name="CreateFahrerMitgliedsNummerTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Geburtsdatum" />
                        <DatePicker x:Name="CreateFahrerGeburtsdatumPicker" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Geschlecht" />
                        <ComboBox x:Name="CreateFahrerGeschlechtComboBox" x:FieldModifier="internal" Height="30" SelectedValuePath="Tag">
                            <ComboBoxItem Content="Maennlich" Tag="m" />
                            <ComboBoxItem Content="Weiblich" Tag="w" />
                            <ComboBoxItem Content="Divers" Tag="d" />
                        </ComboBox>
                        <TextBlock Margin="0,12,0,4" Text="Verein" />
                        <ComboBox x:Name="CreateFahrerVereinComboBox" x:FieldModifier="internal" Height="30" DisplayMemberPath="Name" SelectedValuePath="Id" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <Button Style="{StaticResource RoundedButton}" Background="#1F84DE" Width="120" Height="30" Margin="0,0,8,0" Click="CancelFahrerPage_OnClick" Content="Abbrechen" />
                            <Button Style="{StaticResource RoundedButton}" Background="green" Width="120" Height="30" Click="SaveCreateFahrer_OnClick" Content="Speichern" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border x:Name="FahrerEditPage" x:FieldModifier="internal" Visibility="Collapsed" Background="#F8FAFC" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8">
                    <StackPanel Margin="20">
                        <TextBlock FontWeight="Bold" FontSize="18" Text="Fahrer bearbeiten" />
                        <TextBlock Margin="0,16,0,4" Text="Vorname" />
                        <TextBox x:Name="EditFahrerVornameTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Nachname" />
                        <TextBox x:Name="EditFahrerNachnameTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Mitgliedsnummer" />
                        <TextBox x:Name="EditFahrerMitgliedsNummerTextBox" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Geburtsdatum" />
                        <DatePicker x:Name="EditFahrerGeburtsdatumPicker" x:FieldModifier="internal" Height="30" />
                        <TextBlock Margin="0,12,0,4" Text="Geschlecht" />
                        <ComboBox x:Name="EditFahrerGeschlechtComboBox" x:FieldModifier="internal" Height="30" SelectedValuePath="Tag">
                            <ComboBoxItem Content="Maennlich" Tag="m" />
                            <ComboBoxItem Content="Weiblich" Tag="w" />
                            <ComboBoxItem Content="Divers" Tag="d" />
                        </ComboBox>
                        <TextBlock Margin="0,12,0,4" Text="Verein" />
                        <ComboBox x:Name="EditFahrerVereinComboBox" x:FieldModifier="internal" Height="30" DisplayMemberPath="Name" SelectedValuePath="Id" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <Button Style="{StaticResource RoundedButton}" Background="#1F84DE" Width="120" Height="30" Margin="0,0,8,0" Click="CancelFahrerPage_OnClick" Content="Abbrechen" />
                            <Button Style="{StaticResource RoundedButton}" Background="green" Width="120" Height="30" Click="SaveEditFahrer_OnClick" Content="Speichern" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border x:Name="FahrerDeletePage" x:FieldModifier="internal" Visibility="Collapsed" Background="#F8FAFC" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8">
                    <StackPanel Margin="20">
                        <TextBlock FontWeight="Bold" FontSize="18" Text="Fahrer loeschen" />
                        <TextBlock Margin="0,16,0,0" Text="Soll dieser Fahrer wirklich geloescht werden?" />
                        <TextBlock x:Name="DeleteFahrerTextBlock" x:FieldModifier="internal" Margin="0,10,0,0" FontWeight="SemiBold" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <Button Width="120" Height="30" Margin="0,0,8,0" Click="CancelFahrerPage_OnClick" Content="Abbrechen" />
                            <Button Width="120" Height="30" Background="#DC2626" Foreground="White" BorderThickness="0" Click="ConfirmDeleteFahrer_OnClick" Content="Loeschen" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
